#include <iostream>
#include <cmath>

using namespace std;

int main()
{

    double accuracy = 0.000001;
    double sum = 0;
    double num = 0;
    cin >> num;
    double znmn = 1;
    double sinval = sin(num);
    double general = 0
    do
        {
            general = -general * 
        }
    while (accuracy <= fabs(sinval-sum))
    {
        
    }


    return 0;
}